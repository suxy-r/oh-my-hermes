# AGENTS

This file is for AI agents (Hermes, Claude Code, Codex) working in this project.
Generated by Oh My Hermes bootstrap. Fill in the TODO sections.

---

## Project Overview

TODO: Describe what this project does, who it is for, and what problem it solves.
Keep this to 3-5 sentences. Agents read this to understand project context before taking actions.

## Build Commands

```bash
npm install         # install dependencies
npm run dev         # start development server (localhost:3000)
npm run build       # production build
npm run typecheck   # TypeScript check — run after every change
npm run lint        # lint
```

TODO: Add any project-specific commands (database migrations, seed scripts, etc.)

## Architecture

TODO: Describe data flow, service ownership, and key architectural decisions.

Example structure:
- Frontend: Next.js App Router at /app
- Database: Supabase PostgreSQL with Row Level Security
- Auth: Supabase Auth (email + OAuth)
- Storage: Supabase Storage for file uploads
- Deployment: Vercel (main → production, branches → preview)

Key decisions to document:
- Where is the source of truth for the database schema? (supabase/migrations/)
- Which services communicate with each other and how?
- What are the critical data flows?

## Security Baseline

Agents must follow these requirements for all changes:

- Validate all input on the server — never trust client-provided data
- Never trust client-provided identity, scope, or permissions
- Use Supabase RLS for row-level data isolation — every table needs a policy
- Store secrets in .env.local — never commit credentials or keys
- No secrets in logs, error messages, or API responses
- Enforce auth/authz explicitly on every API route
- Never expose SUPABASE_SERVICE_KEY to the client
- Follow OWASP Top 10

## Engine Guidance

- Route by ambiguity, risk, verification cost, and tool horizon — not file count
- Bounded changes with explicit acceptance criteria → smallest capable coding model
- Ambiguous architecture or long tool loops → strong reasoning model, one end-to-end owner
- Independent reviewers inspect separate evidence; repeated personas are not independent evidence
- Deploy, monitor, notify, schedule, remember context → Hermes
- Product UI/UX and visual verification → Hermes Designer Agent
- Native/authenticated GUI work → computer-use only after API/browser paths
- Not sure? Tell Hermes: "run choose-engine"

For DeepSeek V4: use Flash low for routing/status, Flash high for bounded daily
agent work, Pro high for ambiguous implementation, and Pro max only for rare
high-risk conflict resolution. Use the minimum relevant toolset and preserve
`reasoning_content` throughout thinking-mode tool loops.

## Monitoring

- Health endpoint: GET /api/health → { status: "ok", version, timestamp }
- Error tracking: Sentry (SENTRY_DSN in environment)
- Uptime monitoring: Uptime Kuma polling /api/health every 60 seconds
- Alerts: Slack webhook (SLACK_WEBHOOK_URL in environment)

## Deployment

- Platform: Vercel
- Production: main branch → automatic deploy on push
- Preview: any other branch → automatic preview deployment
- Environment variables: set in Vercel dashboard (not committed to git)
- Database migrations: supabase/migrations/ — pushed via `supabase db push`

## Commit Conventions

- One commit per meaningful change (one feature, one fix, one refactor)
- Never commit .env.local or any file with real credentials
- Always run `npm run typecheck` before committing
- Commit AGENTS.md changes alongside the code changes they document
